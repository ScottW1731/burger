<div class="container my-5">
  <div class="row">

    <div class="col-sm-6 mx-auto">
      <h4 class="text-danger">Your order:</h4>
      <ul>
        {{#each burger}}
        {{#unless eaten}}
            <li>{{name}}</li>
        {{/unless}}
        {{/each}}
      </ul>
    </div>

    <div class="col-smm-6 mx-auto">
      <h4 class="text-danger">Burgers already eaten:</h4>
      <ul>
        {{#each burger}}
        {{#if eaten}}
          <li>{{name}}</li>
        {{/if}}
        {{/each}}
      </ul>
    </div>


    <div class="col-sm-12 text-center">
      <h3 class="py-2">Add a Burger!</h3>
      <form class="create-form">
        <div class="form-group">
          <input type="text" class="btn-lg text-center" id="name" name="name" placeholder="Insert Burger Name">
          <input type="number" id="calories" placeholder="1000">
          <input type="number" id="price" placeholder="10.00">
          <input type="hidden" id="eaten" value="false">
        </div>
        <button class="btn btn-danger p-3" type="submit">Add a burger</button>
      </form>
    </div>

  </div>
</div>